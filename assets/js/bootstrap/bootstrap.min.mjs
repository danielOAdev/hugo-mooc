// Arquivo placeholder. Sera substido na build do site.
console.error(`O módulo ${import.meta.url} não foi montado por algum dos seguintes motivos:
 1. Não foi feita a instalação das dependências do projeto via "npm install";
 2. Recurso não foi publicado via "RESOURCE.Publish";
 3. Má configuração do Hugo. Verifique a seção [[module.mounts]] que copia o módulo para a pasta "./assets".
`);
